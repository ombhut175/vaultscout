# Environment Variables Configuration
# 
# PRIORITY ORDER:
# 1. .env.local (highest priority - local development overrides)
# 2. .env (fallback - shared configuration)
#
# Create .env.local for local development overrides
# Create .env for shared/team configuration

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Configuration (PostgreSQL)
# Use either DATABASE_URL or individual connection parameters
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
# Alternative: Individual connection parameters
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=database_name
# DATABASE_USER=username
# DATABASE_PASSWORD=password

# Application Configuration
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:3656
REDIRECT_TO_FRONTEND_URL=http://localhost:3656/login

# Cookie Configuration for Production
# Set the domain for cookies in production (e.g., .yourdomain.com for cross-subdomain support)
# Only needed if frontend and backend are on different subdomains
# COOKIE_DOMAIN=.yourdomain.com



# Swagger / API Docs (non-production recommended)
# Enable/disable swagger generation and UI (NODE_ENV=production will still disable by default in code)
# SWAGGER_ENABLED=true
# Protect Swagger UI with basic auth by setting both variables
# SWAGGER_USER=admin
# SWAGGER_PASSWORD=super-secure-password
# Swagger UI preferences
# SWAGGER_UI_DEEP_LINKING=true
# SWAGGER_UI_DOC_EXPANSION=none # values: list | full | none (keep tags closed)
# SWAGGER_UI_FILTER=true        # enables search box (can be "true" or a string to filter by default)